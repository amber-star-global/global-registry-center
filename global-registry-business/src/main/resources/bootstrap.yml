spring:
  application:
    name: registry-center
  profiles:
    active: dev
  cloud:
    config:
      label: master
      discovery:
        enabled: false
        service-id: config-center
      fail-fast: true
      username: admin
      password: as123456

#management:
#  security:
#    enabled: false

# 启动参数: spring.profiles.active=选择拉取配置文件; spring.cloud.config.uri=注册中心地址